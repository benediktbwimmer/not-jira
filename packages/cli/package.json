{
  "name": "@unblock/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "unblock": "dist/index.js"
  },
  "scripts": {
    "prebuild": "npm run build -w @unblock/core",
    "build": "tsc -p tsconfig.json",
    "precheck": "npm run build -w @unblock/core",
    "check": "tsc -p tsconfig.json --noEmit",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {
    "@unblock/core": "0.1.0",
    "commander": "^14.0.2"
  }
}
